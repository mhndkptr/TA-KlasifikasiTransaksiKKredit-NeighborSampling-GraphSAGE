# Start with CPU feature storage: full one-hot features are larger than EXP9.
extends: config.yaml
runtime:
  feature_device: cpu
  cpu_threads: 8
  progress_update_batches: 100
training:
  batch_size: 4096
sampling:
  edge_chunk_size: 500000
evaluation:
  batch_size: 8192
