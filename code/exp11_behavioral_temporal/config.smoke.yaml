extends: config.yaml
experiment:
  name: exp11_smoke
  seeds: [42]
  max_rows: 1000000
training:
  min_epochs: 1
  epochs: 3
  batch_size: 256
evaluation:
  batch_size: 1024
  calibration_tail_fraction: 0.25
paths:
  model_dir: ../../model/exp11_smoke
  result_dir: ../../result/exp11_smoke
